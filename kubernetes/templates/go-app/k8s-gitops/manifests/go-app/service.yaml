apiVersion: v1
kind: Service
metadata:
  name: __SVC__
  namespace: __SVC__
  labels:
    app.kubernetes.io/name: __SVC__
spec:
  selector:
    app.kubernetes.io/name: __SVC__
  ports:
    - name: http
      port: 8080
      targetPort: http
